# Copy to .env and fill in. .env is gitignored.

# Claude API key (provided with the challenge testing budget).
ANTHROPIC_API_KEY=

# Optional: pin models used by the pipeline (defaults set in src/leo_pipeline/config.py)
# LEO_DRIVER_MODEL=claude-opus-4-8
# LEO_WORKER_MODEL=claude-sonnet-4-6

# Optional: dataset endpoints / API keys added as agents are built
# (e.g. canopy-height, terrain DEM, building-footprint sources)
